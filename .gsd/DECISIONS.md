# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Core tech stack for FICCC website rebuild | Astro + Sanity.io + Vercel | Astro provides static-first output with island architecture for interactive components. Sanity provides headless CMS with free tier covering 20 users and 10K documents. Vercel provides edge CDN deployment with free tier, preview deploys, and built-in analytics. Stack delivers zero-server-maintenance goal from PRD. | Yes | collaborative |
| D002 |  | architecture | Analytics platform | Vercel Web Analytics (free tier, 2500 events/day) | Zero cost, zero maintenance, already in the Vercel stack. Privacy-friendly, no cookie banner needed. Sufficient for church site traffic. GA4 available as future backup if deeper funnel analysis needed. | Yes | collaborative |
| D003 |  | architecture | Bilingual content architecture | Separate documents with language field, not i18n field duplication | EN and ZH content is bespoke per ministry, not translations. CM sermons are different sermons, CM fellowships are different groups. Language field on documents with GROQ filtering is simpler and lets each ministry own content independently. Shared nav structure with language toggle at route level (/ for EN, /zh/ for ZH). | Yes | collaborative |
| D004 |  | library | Search technology | Pagefind (static build-time index) | Zero runtime cost, builds index at static generation time, ~5KB client bundle, supports CJK characters for Chinese content. Perfect fit for Astro static output. | Yes | collaborative |
| D005 |  | architecture | Sermon hosting and delivery | YouTube embeds via @FICCCenglish channel, Sanity stores metadata + video ID | Church already uploads weekly sermons to YouTube with structured naming (date, title, scripture). YouTube handles video hosting/bandwidth at zero cost. Sanity stores sermon metadata (title, speaker, series, scripture, YouTube ID) for filtering and search. No need for self-hosted audio/video. | Yes | collaborative |
| D006 |  | design | Logo treatment | Humanist serif wordmark in both EN and ZH (no graphic logo) | No existing graphic logo. Text-mark in the serif headline font is consistent with the 'honest, not branded' design direction. Church name in both languages: 'First Ithaca Chinese Christian Church' / '伊的家華人基督教會'. Graphic logo may be created later. | Yes | collaborative |
| D007 |  | design | Design language and visual direction | "Gathered Warmth" — humanist serif headlines, warm neutral palette, real candid photography, unhurried spacing | Design Language doc defines 6 tone descriptors (Unhurried, Rooted, Textured, Legible, Familiar, Honest) and the core frame of 'long-table hospitality' vs 'venue hospitality'. Typography: humanist serif (Lora/Cormorant) for headlines + clean sans (Inter/DM Sans) for body + CJK serif pairing. Warm neutrals (off-white, warm stone) with muted terracotta + deep ink primary. Real FICCC community photos are a hard requirement. | Yes | collaborative |
| D008 |  | performance | How to handle CJK font CSS blocking render | Split Noto Serif SC @font-face rules into separate CSS file (cjk-fonts.css) loaded async via media=print onload=this.media='all' pattern | Fontsource's Noto Serif SC generates 240 @font-face rules (120 unicode-range subsets × 2 weights) totaling 240KB CSS. Bundled into the main stylesheet, this blocked render and dropped Lighthouse perf to 0.57. Async loading keeps critical CSS at 8KB while CJK fonts still load via font-display: swap. | Yes | agent |
| D009 |  | performance | Lighthouse CI performance thresholds per page type | Per-URL assertMatrix: production pages ≥ 0.75, dev-only pages (styleguide) ≥ 0.50 (warn only) | CJK font subsetting generates 240KB CSS causing Lighthouse perf scores of 0.59 on the styleguide (which renders heavy CJK text) and 0.76-0.77 on the home page under simulated 4G throttling. A single global threshold either flakes on real pages or suppresses meaningful regressions. Per-URL thresholds match actual page characteristics. Styleguide is a dev reference, not user-facing — warn is sufficient. | Yes | agent |
| D010 |  | architecture | Component architecture for shared UI components | Pure Astro components with scoped CSS using only CSS custom properties from global.css. No client-side JS framework. Interactive behavior via native HTML (details/summary) or minimal inline scripts (hamburger toggle). | All 8 base components (Header, Footer, Hero, Card, ImageMosaic, Accordion, ContactForm, AudioPlayer) built without React/Vue/Svelte. Native HTML elements handle interactivity (details/summary for accordion, audio controls for player). Only the mobile hamburger needs a small inline script. This keeps bundle size near zero and Lighthouse scores high. | Yes | agent |
| D011 | M001/S04/T01 | architecture | Sanity Studio embedding strategy for static Astro site | Manual client:only="react" component at /admin instead of @sanity/astro studioBasePath | @sanity/astro's studioBasePath injects a server-rendered route requiring output:'server' or 'hybrid' with an adapter. Manual embed via Studio.tsx + client:only keeps the site fully static. | Yes | agent |
| D012 | M001/S05/T02 | security | HSTS policy for production deployment | Strict-Transport-Security: max-age=63072000; includeSubDomains; preload | 2-year max-age with includeSubDomains and preload per OWASP best practice. Applied via vercel.json global headers alongside existing CSP, X-Frame-Options, and other security headers. | Yes | agent |
| D013 | M001/S06 | architecture | Output mode for Astro site | Switch from `output: 'static'` to `output: 'hybrid'` with `@astrojs/vercel` adapter | Sanity Visual Editing requires server-rendered routes to fetch drafts at request time. Hybrid mode keeps all existing pages statically pre-rendered by default (zero behavior change) while enabling opt-in SSR. This supersedes the K004/D011 rationale for staying fully static — the manual Studio embed at /admin still works fine under hybrid mode. | No — preview mode fundamentally requires server capability | agent |
| D014 | M001/S06/T01 | architecture | Output mode for Astro 5 site with Vercel adapter | Keep `output: 'static'` with `@astrojs/vercel@9` adapter — Astro 5 removed `hybrid` mode; static + adapter provides identical per-page SSR opt-in via `export const prerender = false` | Astro 5 merged hybrid into static: adding an adapter to a static site enables per-page SSR opt-in without a separate mode. D013 planned for hybrid but this is the Astro 5 equivalent. @astrojs/vercel@9 used because v10 requires Astro 6. | No — determined by Astro version | agent |
| D015 |  | infrastructure | LHCI SEO threshold for 404 page | Set 404 page SEO assertion to warn at 0.8 instead of error at 0.95 | 404 pages inherently score lower on SEO (no meaningful content, noindex intent) so a strict 0.95 threshold would cause false failures in CI. | Yes | agent |
