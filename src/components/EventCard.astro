---
interface Props {
  title: string;
  date: string;
  time?: string;
  location?: string;
  image?: string;
  recurring?: boolean;
}

const { title, date, time, location, image, recurring } = Astro.props;

/** Format an ISO date string to a readable form, e.g. "Sunday, April 12, 2026" */
function formatDate(iso: string): string {
  try {
    const d = new Date(iso);
    return d.toLocaleDateString('en-US', {
      weekday: 'long',
      year: 'numeric',
      month: 'long',
      day: 'numeric',
    });
  } catch {
    return iso;
  }
}

const displayDate = formatDate(date);
---

<div class="event-card">
  {image && (
    <div class="event-card__image-wrap">
      {/* Decorative: card title provides text alternative */}
      <img class="event-card__image" src={image} alt="" loading="lazy" />
    </div>
  )}
  <div class="event-card__body">
    <div class="event-card__header">
      <h3 class="event-card__title">{title}</h3>
      {recurring && <span class="event-card__badge">Recurring</span>}
    </div>
    <p class="event-card__date">{displayDate}</p>
    {time && <p class="event-card__meta">{time}</p>}
    {location && <p class="event-card__meta">{location}</p>}
  </div>
</div>

<style>
  .event-card {
    display: flex;
    flex-direction: column;
    border-radius: var(--radius-md);
    border: 1px solid var(--color-stone);
    background-color: var(--color-bg);
    overflow: hidden;
  }

  .event-card__image-wrap {
    aspect-ratio: 3 / 2;
    overflow: hidden;
  }

  .event-card__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .event-card__body {
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .event-card__header {
    display: flex;
    align-items: baseline;
    gap: var(--space-3);
    flex-wrap: wrap;
  }

  .event-card__title {
    font-family: var(--font-heading);
    font-size: var(--text-lg);
    line-height: var(--leading-tight);
    color: var(--color-ink);
  }

  .event-card__badge {
    display: inline-block;
    font-family: var(--font-body);
    font-size: var(--text-xs, 0.75rem);
    font-weight: 600;
    color: var(--color-bg);
    background-color: var(--color-terracotta);
    padding: 0.125rem var(--space-2);
    border-radius: var(--radius-sm, 0.25rem);
    white-space: nowrap;
    line-height: 1.4;
  }

  .event-card__date {
    font-family: var(--font-body);
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-terracotta);
    line-height: var(--leading-relaxed);
  }

  .event-card__meta {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    color: var(--color-body);
    line-height: var(--leading-relaxed);
  }
</style>
